## What and why

<!-- What changes, and what problem it solves. Link an issue if there is one. -->

## Checklist

- [ ] `pnpm typecheck && pnpm test` pass locally
- [ ] Added tests that would fail without this change
- [ ] Kept the invariants: fails open when detached, never throws into the host app
- [ ] Docs updated if behavior changed (`apps/docs/`)
