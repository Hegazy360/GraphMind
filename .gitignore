# deps / build
node_modules/
dist/
*.tsbuildinfo

# env & secrets
.env
.env.*
*.local

# vercel
.vercel/

# internal strategy docs — never published
internal/

# os / editor
.DS_Store

# viewer build artifact copied into cli for serving
packages/cli/viewer-dist/
